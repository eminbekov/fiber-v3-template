# Copy to .env and adjust. Never commit real secrets.

# Docker Compose project name (used for container grouping in Docker Desktop).
COMPOSE_PROJECT_NAME=fiber-template

# development | production
ENVIRONMENT=development

# debug | info | warn | error
LOG_LEVEL=debug

# HTTP listen address (host:port or :port)
HTTP_LISTEN_ADDRESS=:8080

# [module:views:start]
# Path to HTML templates directory.
VIEWS_PATH=./views
# [module:views:end]

# Comma-separated allowed origins for CORS. Empty means deny cross-origin browser requests.
CORS_ALLOW_ORIGINS=

# Maximum request body size in bytes. Default is 4194304 (4 MB).
BODY_LIMIT=4194304

# OpenTelemetry collector endpoint (host:port, no scheme). Empty disables telemetry export.
# Local dev: leave empty. Docker Compose stack: use otel-collector:4317 (see deploy/docker/docker-compose.yml).
OTEL_EXPORTER_ENDPOINT=

# --- PostgreSQL ---
# Individual vars: consumed by docker-compose to create the Postgres container.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=fiber_template
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Assembled URL: consumed by the Go app to connect.
DATABASE_URL=postgres://postgres:postgres@postgres:5432/fiber_template?sslmode=disable

# --- Redis ---
# Individual vars: consumed by docker-compose.
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis_password

# Assembled URL: consumed by the Go app.
REDIS_URL=redis://:redis_password@redis:6379/0

# [module:nats:start]
# --- NATS ---
# Individual vars: consumed by docker-compose.
NATS_HOST=nats
NATS_PORT=4222
NATS_USER=nats_user
NATS_PASSWORD=nats_password

# Assembled URL: consumed by the Go app.
NATS_URL=nats://nats_user:nats_password@nats:4222
# [module:nats:end]

# [module:grpc:start]
# gRPC listen address (host:port or :port).
GRPC_LISTEN_ADDRESS=:9090
# [module:grpc:end]

# Session duration (Go duration format).
SESSION_DURATION=24h

# [module:storage:start]
# Storage: local | s3
STORAGE_TYPE=local

# Local filesystem root for uploads when STORAGE_TYPE=local.
STORAGE_LOCAL_BASE_PATH=./uploads

# S3-compatible API (AWS S3 or MinIO) when STORAGE_TYPE=s3.
# For AWS, leave S3_ENDPOINT empty. For MinIO use e.g. http://localhost:9000.
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=

# Optional public URL prefix for CDN or reverse-proxy (no trailing slash).
CDN_BASE_URL=

# Secret for HMAC-signed file URLs (development example only; use a strong random value in production).
FILE_SIGNING_KEY=change-me-in-production-use-long-random-secret

# Lifetime for signed download URLs (Go duration format, e.g. 15m, 1h).
SIGNED_URL_TTL=15m
# [module:storage:end]
